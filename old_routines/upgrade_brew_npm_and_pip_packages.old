python -m pip install -U pip
brew update
brew upgrade
pip --disable-pip-version-check list --outdated --format=json | python -c "import json, sys; print('\n'.join([x['name'] for x in json.load(sys.stdin)]))"
brew upgrade homebrew-ffmpeg/ffmpeg/ffmpeg
brew cleanup
npm update -g
